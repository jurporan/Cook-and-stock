<%= form_for(@dish_order) do |f| %>
  <% if @dish_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dish_order.errors.count, "error") %> prohibited this dish_order from being saved:</h2>

      <ul>
      <% @dish_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dish_id %><br>
    <%= f.number_field :dish_id %>
  </div>
  <div class="field">
    <%= f.label :order_id %><br>
    <%= f.number_field :order_id %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br>
    <%= f.number_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
